#! /usr/bin/python3
from logging import getLogger
import argparse

logger = getLogger(__name__)

if __name__ == '__main__':
    parser = argparse.ArgumentParser(
        description="A description of the cli app"
    )
